apiVersion: v1
kind: Service
metadata:
  labels:
    app: frontend-client
  name: frontend-client
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    app: frontend-client
  type: "ClusterIP"